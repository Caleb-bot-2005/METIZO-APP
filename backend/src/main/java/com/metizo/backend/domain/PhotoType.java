package com.metizo.backend.domain;

/**
 * Work-proof photo category: artisans upload BEFORE and AFTER shots of a job
 * to build credibility and let customers compare results.
 */
public enum PhotoType {
    BEFORE,
    AFTER
}
