package com.metizo.backend.domain;

/** Mirrors the frontend's NotificationCategory union. */
public enum NotificationCategory {
    BID,
    ARRIVAL,
    PAYMENT,
    JOB,
    EMERGENCY,
    PROMOTION
}
